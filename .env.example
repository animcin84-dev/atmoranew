# Optional production lead endpoint. Leave unset for the explicit local-demo mode.
# The endpoint must accept a JSON POST from the website origin and return a 2xx response.
VITE_ATMORA_ASSESSMENT_ENDPOINT=

# Optional canonical origin used by the prerender script for canonical/OG URLs and sitemap.xml.
ATMORA_SITE_URL=
