import { MAINTENANCE } from '../../data/maintenance'
import { RouteLink } from '../../components/RouteLink'
import { StatusChip } from '../components/StatusChip'
export function MaintenancePage(){return <div className="console-page maintenance-page"><section className="maintenance-lead"><span className="console-kicker">Service planning / SYNTHETIC DEMO DATA</span><h2>KEEP THE<br/>WATER PATH<br/><em>LEGIBLE.</em></h2><p>Component-life values are illustrative frontend states, not predictive maintenance claims.</p></section><section className="maintenance-list">{MAINTENANCE.map(item=><article key={item.id}><div className="maintenance-list__meta"><StatusChip status={item.state}/><RouteLink to={`/app/devices/${item.deviceId}`}>{item.deviceId} ↗</RouteLink></div><div><h3>{item.component}</h3><p>{item.note}</p></div><div className="life-bar" role="progressbar" aria-label={`${item.remainingPercent}% illustrative remaining life`} aria-valuenow={item.remainingPercent} aria-valuemin={0} aria-valuemax={100}><i style={{width:`${item.remainingPercent}%`}}/><span>{item.remainingPercent}%</span></div><strong>{item.due}</strong></article>)}</section></div>}
