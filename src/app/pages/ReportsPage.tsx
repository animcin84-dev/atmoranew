import { useState } from 'react'
import { REPORTS } from '../../data/reports'
export function ReportsPage(){const [preview,setPreview]=useState<string|null>(null);return <div className="console-page reports-page"><section className="reports-lead"><span className="console-kicker">Evidence library / FRONTEND PREVIEWS</span><h2>REPORTS<br/>WITHOUT<br/><em>THEATRE.</em></h2><p>No PDF is claimed to exist until a real document is connected.</p></section><section className="report-library">{REPORTS.map(report=><article key={report.id} data-open={preview===report.id}><div className="report-library__index mono">{report.id}</div><div><span>{report.type}</span><h3>{report.title}</h3><p>{report.summary}</p></div><strong>{report.period}</strong><button type="button" onClick={()=>setPreview(preview===report.id?null:report.id)}>{preview===report.id?'Close preview':'Preview'}</button>{preview===report.id&&<div className="report-preview"><span className="console-kicker">Synthetic preview</span><p>This frontend demonstrates the hierarchy and report object only. There is no generated file or server-side report.</p><button type="button" disabled>Download unavailable in frontend demo</button></div>}</article>)}</section></div>}
